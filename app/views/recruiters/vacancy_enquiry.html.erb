<%= @vacancy.title %>
<%= @vacancy.description %>

<%= form_tag(recruiters_vacancy_enquiry_message_path(@vacancy), method: :post) do %>
  <%= label_tag(:message, "Message:") %>
  <%= text_field_tag(:message) %>
  <%= submit_tag("Send") %>
<% end %>
